#include <stdio.h>
int main(void) {
  int no,arr[100],i,j,sum1=0,sum2=0,no1=0,no2=0,average1,average2;
  scanf("%d",&no);
  for(i=0;i<no;i++){
    scanf("%d",&arr[i]);
  }
  for(i=0;i<no/2;i++)
  {
    sum1+=a[i];
    no1++;
  }
  for(i=no/2;i<n;i++)
  {
    sum2+=arr[i];
    no2++;
  }
  average1=sum1/no1;
  average2=sum2/no2;
  if(average1==average2)
   printf("Possible");
  else
    printf("Not possible");
  return 0;
}
